import styled from 'styled-components/native';

const TaskTitle = styled.TextInput`
  border: 1px;
  border-radius: 10px;
  font-size: larger;
  padding: 10px;
`;

const ButtonContainer = styled.View`
  flex: 1;
  padding: 2px;
`;

export { TaskTitle, ButtonContainer };
